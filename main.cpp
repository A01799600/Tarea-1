#include <iostream>

using namespace std;

double calcularIMC(double p, double a){
    return p/ (a * a);
}

int main(void){

    double peso,altura;
    cout << "Ingrese el peso (Kg): " << endl;
    cin >> peso;
    cout << "Ingrese la altura (m): " << endl;
    cin >> altura;
    
}